@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    <li>
        <a href="/Manage/Index" title="Manage User Profile"><i class="fa fa-user fa-fw"></i>User Profile</a>
    </li>
    <li class="divider">
    </li>
    <li>
        @using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm" }))
        {
            @Html.AntiForgeryToken()
            <a href="javascript:document.getElementById('logoutForm').submit()"><i class="fa fa-sign-out fa-fw"></i>Logout</a>
        }
    </li>


    @*<ul class="nav navbar-nav navbar-right">
                                <li>
                                    @Html.ActionLink("Hello " + User.Identity.GetUserName() + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })
                                </li>
                                <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
                            </ul>
        }*@
}
else
{
    <li>
        <a href="/Account/Register" id="registerLink"><i class="fa fa-id-card-o fa-fw"></i>Register</a>
    </li>
    <li>
        <a href="/Account/Login" id="loginLink"><i class="fa fa-sign-in fa-fw"></i>Log in</a>
    </li>
}
